# Fabspeed BigCommerce Theme
A work in progress. Somehow it works with Node LTS. Although any saved JS changes kinda break the local server, so needs to be reloaded when saving js files.

## NPM Scripts:
- `npm run live` / `npm run sandbox` respectively will use the data from each site